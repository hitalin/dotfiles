#!/bin/sh

exists() {
    which "$@" >/dev/null 2>&1
}

if exists gvfs-trash; then
    exec gvfs-trash "$@"

elif exists trash-put; then
    exec trash-put "$@"

elif exists rmtrash ; then
    exec rmtrash "$@"

else
    echo 'please install a tool to use trash' >&2
    exit 1

fi
