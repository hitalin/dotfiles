# ツールが自動インストールする vendored スキル（cloudflare 系など）は追跡しない。
# 自作スキルのみを allowlist で明示追跡する。
#
# 新しい自作スキルを追加したら、ここに `!<name>/` を追記すること
# （追記しないと git に無視されコミットされない）。
/*
!.gitignore
!commit/
!deps/
!fix-issue/
!refactor/
!review-pr/
!security-audit/
!test/
